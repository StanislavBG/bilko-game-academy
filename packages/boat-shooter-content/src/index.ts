/**
 * @bilko/boat-shooter-content — static content data files for Boat Shooter.
 *
 * Consumers import the JSON files directly via the package's `./data/*.json`
 * subpath exports (see package.json `exports`). This module exists only to
 * satisfy the package's main entry; it currently has no runtime API.
 */
export {};
